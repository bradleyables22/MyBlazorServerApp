@model IEnumerable<MyBlazorServerApp.Data.Models.GuestEntry>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GuestName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EntryTimeDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GuestInput)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GuestName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EntryTimeDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GuestInput)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GuestEntryID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GuestEntryID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GuestEntryID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
